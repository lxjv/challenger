---
import text from "@data/flavourtext.json";
import { getRandomItem } from "@lib/filters/getRandom";
const quotes = text.quotes;
---

<blockquote>
    {
        getRandomItem(quotes)
    }
</blockquote>

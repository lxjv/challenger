---
import Layout from "./PageLayout.astro";
const { frontmatter: f } = Astro.props;
let fixedDate: Date 
if (f.lastUpdated) {
    fixedDate = new Date(f.lastUpdated)
}
---

<Layout
    pageTitle={f.pageTitle}
    description={f.description}
    back={f.back}
    icon={f.icon}
    uc={f.uc}
    lastUpdated={fixedDate}
>
    <slot />
</Layout>